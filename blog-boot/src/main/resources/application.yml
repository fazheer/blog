spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog
    username: root
    password: Myh@1346789.
  main:
    banner-mode: off
  data:
    redis:
      host: localhost
      port: 6379
#      password: ${REDIS_PASSWORD:myh134679}
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 100
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  mvc:
    format:
      date: yyyy-MM-dd HH:mm:ss

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
  mapper-locations: classpath:mapper/*.xml
# 安全配置（生产环境请务必通过环境变量注入）
jwt:
  secret: ${JWT_SECRET:mhdksdu@!@G}

obs:
  bucket-name: myh-blog-bucket
  endpoint: https://obs.cn-north-1.myhuaweicloud.com
#  ak:
#  sk:
  domain: https://obs.jiaoker.cn

cors:
  allowed-origins: CORS_ALLOWED_ORIGINS:*

server:
  port: 6008
